--- origsrc/roxterm-1.15.2/configure.ac	2009-07-04 18:57:47.000000000 -0500
+++ src/roxterm-1.15.2/configure.ac	2009-10-08 01:56:44.964635000 -0500
@@ -95,7 +95,7 @@ PKG_CHECK_MODULES(glib, [glib-2.0 >= 2.6
 PKG_CHECK_MODULES(gtk, [gtk+-2.0 >= 2.6.0])
 PKG_CHECK_MODULES(dbus, [dbus-glib-1 >= 0.22])
 CFLAGS="$CFLAGS $glib_CFLAGS $gtk_CFLAGS $dbus_CFLAGS"
-LDFLAGS="$LDFLAGS $glib_LIBS $gtk_LIBS $dbus_LIBS"
+LIBS="$LIBS $glib_LIBS $gtk_LIBS $dbus_LIBS"
 
 dnl Terminal only
 PKG_CHECK_MODULES(vte, [vte >= 0.11.11])
@@ -134,12 +134,12 @@ TH_CHECK_ALTERNATIVE_FUNCS(dbus-1,
 
 AC_CHECK_FUNCS_ONCE(g_mkdir_with_parents gtk_file_chooser_button_new g_file_set_contents get_current_dir_name)
 tmp_CFLAGS="$CFLAGS"
-tmp_LDFLAGS="$LDFLAGS"
+tmp_LIBS="$LIBS"
 CFLAGS="$vte_CFLAGS"
-LDFLAGS="$vte_LIBS"
+LIBS="$vte_LIBS"
 AC_CHECK_FUNCS(vte_terminal_set_cursor_blink_mode vte_terminal_match_add_gregex vte_terminal_set_cursor_shape)
 CFLAGS="$tmp_CFLAGS"
-LDFLAGS="$tmp_LDFLAGS"
+LIBS="$tmp_LIBS"
 AM_CONDITIONAL(HAVE_G_FILE_SET_CONTENTS, test x$HAVE_G_FILE_SET_CONTENTS == 1)
 
 #if test x$enable_gtk_native_tab_dragging = xyes
